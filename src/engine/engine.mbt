///|
/// Compatibility facade for layout computation.
/// Keeps TUI-side imports on `mizchi/layout/*`.

///|
pub using @cengine {
  compute_layout,
  compute_layout_with_warnings,
  compute_block_layout,
  compute_flex_layout,
  compute_element_shift,
  compute_total_cls,
}
